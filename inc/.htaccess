# Block ALL web access to CEM environment secrets.
# PHP can still read these files server-side; only HTTP access is denied.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
