# Block ALL web access to runtime data (admin password hash, GitHub token, logs).
# PHP can still read/write these files server-side; only HTTP access is denied.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
